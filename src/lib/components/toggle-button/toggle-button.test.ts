import { describe, it, expect } from 'vitest';
import { ToggleButton } from './index.js';

describe('ToggleButton', () => {
	it('should be a function', () => {
		expect(typeof ToggleButton).toBe('function');
	});
});
