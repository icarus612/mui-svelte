import { describe, it, expect } from 'vitest';
import { TransferList } from './index.js';

describe('TransferList', () => {
	it('should be a function', () => {
		expect(typeof TransferList).toBe('function');
	});
});
