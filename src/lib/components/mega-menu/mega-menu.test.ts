import { describe, it, expect } from 'vitest';
import { MegaMenu } from './index.js';
describe('MegaMenu', () => {
	it('should be a function', () => {
		expect(typeof MegaMenu).toBe('function');
	});
});
