import { describe, it, expect } from 'vitest';
import { SpeedDial } from './index.js';
describe('SpeedDial', () => {
	it('should be a function', () => {
		expect(typeof SpeedDial).toBe('function');
	});
});
