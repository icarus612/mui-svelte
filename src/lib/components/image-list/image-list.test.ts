import { describe, it, expect } from 'vitest';
import { ImageList } from './index.js';
describe('ImageList', () => {
	it('should be a function', () => {
		expect(typeof ImageList).toBe('function');
	});
});
